<Window x:Class="Areopag.WPF.PowerCalc.View.DrivesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Areopag.WPF.PowerCalc.ViewModels"
        mc:Ignorable="d"
        Title="Перечень приводных механизмов" Height="auto" Width="auto" WindowStartupLocation="CenterScreen" d:DesignWidth="250">
    <Window.DataContext>
        <vm:DrivesWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                Value="{Binding RelativeSource={RelativeSource Self},
                    Path=(Validation.Errors)[0].ErrorContent}" />
                    <Setter Property="Foreground" Value="Red" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <DataGrid x:Name="AboutDrivesGrid" ItemsSource="{Binding Path=AboutDrives}"/>
    </Grid>
</Window>
